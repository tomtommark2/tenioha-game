# プロジェクト引継ぎ資料 (To: Next AI Agent)

**⚠️⚠️ 重要: 作業を開始する前に、必ずこのファイル (`PROJECT_HANDOVER.md`) を熟読してください！ ⚠️⚠️**
**このファイルを読まずに作業を進めると、既存のルール（HTML同期やファイル構成）を破壊する恐れがあります。**

## 1. プロジェクト概要
*   **名称:** 英単語学習クリッカーゲーム (Vocab Clicker Game)
*   **目的:** クリッカーゲーム形式で英単語を学習できるWebアプリ。
*   **現状:** Web版としてGitHub Pages/Firebase Hostingで運用中。PWA対応済み。

## 2. 直近の開発内容 (v2.60 -> v2.63)
以下の機能実装とバグ修正を行いました。

### ✅ 新機能
1.  **学習の記録「推定語彙数」**: 学習進捗からEikenレベルと推定語彙数を算出して表示。
2.  **プレミアム差別化 (Learning Log Differentiation)**:
    *   主要な統計（診断・グラフ）は無料公開。
    *   **詳細分析（成長速度、弱点克服数、予測）はプレミアム限定（🔓）**とし、非課金ユーザーにはモザイク処理/ロックアイコンを表示。
3.  **無限集中モード (Infinite Focus Mode)**:
    *   3〜10秒の制限時間内に回答し続ける「サバイバルモード」を追加（Helpメニュー内）。
    *   学習時間ランキング（`leaderboard_focus_weekly`）の実装。

### ✅ 重要なバグ修正 & アーキテクチャ変更
1.  **平均学習数のバグ修正 (Pending Deploy)**:
    *   現象: 1問解くと学習数が3増える（三重計上）。
    *   原因: UI二重加算、イベントリスナー多重登録、コード重複。
    *   対応: `game_logic.js` / `ui_manager.js` を修正済み。**ローカル確認OK、未デプロイ**。
2.  **F5リロード問題**:
    *   `run_local_server.bat` / `local_server.py` を作成し、ローカル開発時の強力なキャッシュ無効化 (`Cache-Control: no-cache`) を実現。
    *   変数宣言を `let` -> `var` に戻し、再読み込み時の衝突エラーを解消。
3.  **Firestore権限**:
    *   `firestore.rules` に `leaderboard_focus_weekly` を追加し、ランキングエラーを解消。

## 3. 次のアクション (Next Steps)
**最優先: v2.64 (またはHotfix) のデプロイ**

現在、ローカル (`localhost`) では「平均学習数のバグ」が修正されていますが、**本番環境 (`tenioha-game.web.app`) にはまだ反映されていません。**
次のセッションでは、まず以下の手順でデプロイを行ってください。

1.  `js/version.js` を `v2.64` に更新。
2.  `index.html` の読み込みバージョン（`?v=...`）を更新。
3.  `service_worker.js` の `CACHE_NAME` を更新。
4.  `firebase deploy` を実行。

## 4. 技術スタック & 重要ルール
*   **HTML同期:** `vocab_clicker_game.html` (Master) -> `index.html` (Replica)。必ず同期すること。
*   **読み込み順序:** `version` -> `config` -> `utils` -> `game_logic` -> `ui_manager` -> `firebase_app_v2`。
*   **デザイン:** Premium & Rich AI Design。単純なUIはNG。

---
**Good Luck!**
