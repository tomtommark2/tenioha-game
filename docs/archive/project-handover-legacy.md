# Legacy Project Handover

This file preserves the old handover note as archive material.

Use it only for historical reference. Current code, tests, and the standard docs in the repo take precedence.

Useful points already absorbed into the current docs:

- `index.html` is the master HTML file
- `vocab_clicker_game.html` is the synchronized copy
- version changes are anchored in `js/version.js`
- sync and test checks should run before release
