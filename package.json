{
  "scripts": {
    "sync:html": "node scripts/sync-html.js",
    "check:html-sync": "node scripts/check-html-sync.js",
    "test:unit": "playwright test --config playwright.unit.config.js",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:headed": "playwright test --headed",
    "test:e2e:safe": "npm run sync:html && npm run check:html-sync && npm run test:unit && playwright test"
  },
  "dependencies": {
    "firebase-tools": "^15.2.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}
